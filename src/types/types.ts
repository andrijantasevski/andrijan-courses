export interface Course {
  courseTitle: string;
}

export interface Module {
  moduleTitle: string;
  moduleOrder: number;
}

export interface Lesson {
    lessonTitle: string;
    lessonOrder: number;
  }